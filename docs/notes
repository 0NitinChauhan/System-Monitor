1. UpTime function for process not used --> rather UpTime for process calculated inside ProcessClass

2. Jiffy related functions not implemented in LinuxParser. Instead, a single function which gets all JiffyValues is implemented